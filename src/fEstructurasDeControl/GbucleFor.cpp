#include <iostream>

using namespace std;

int g_bucle_for() { //g_bucle_for

	for (int i = 0; i < 20; i++) { //La variable i empieza en 0, se repite mientras sea menor que 20 y en cada iteración se incrementa en una unidad la variable i.
		cout << i << endl;
	}

	return 0;

}
